
{
  "entities": {
    "Post": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Post",
      "type": "object",
      "description": "Represents a post in the AnonyCollab application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Post entity."
        },
        "postType": {
          "type": "string",
          "enum": [
            "general",
            "qa",
            "feedback"
          ],
          "description": "The type of the post."
        },
        "title": {
          "type": "string",
          "description": "The title or question of the post."
        },
        "problemDetails": {
          "type": "string",
          "description": "Detailed description of the problem."
        },
        "problemSummary": {
          "type": "string",
          "description": "A short summary of the problem."
        },
        "whatIveTried": {
          "type": "string",
          "description": "Details on what has been tried to solve the problem."
        },
        "whatIveTriedSummary": {
          "type": "string",
          "description": "A short summary of what has been tried."
        },
        "expectedOutcome": {
          "type": "string",
          "description": "Details on the expected outcome."
        },
        "expectedOutcomeSummary": {
          "type": "string",
          "description": "A short summary of the expected outcome."
        },
        "sector": {
          "type": "string",
          "description": "The industry sector the post relates to."
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of tags associated with the post."
        },
        "imageUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL of an optional image for the post."
        },
        "author": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "avatar": {
              "type": "string",
              "format": "uri"
            }
          },
          "required": [
            "name",
            "avatar"
          ]
        },
        "likes": {
          "type": "integer"
        },
        "comments": {
          "type": "integer"
        },
        "reposts": {
          "type": "integer"
        },
        "shares": {
          "type": "integer"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the post was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "title",
        "createdAt"
      ]
    },
    "Project": {
      "title": "Project",
      "description": "Represents a collaborative project in the Discover section.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the project."
        },
        "title": {
          "type": "string",
          "description": "The title of the project."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the project."
        },
        "image": {
          "type": "string",
          "format": "uri",
          "description": "URL for the project's cover image."
        },
        "sector": {
          "type": "string",
          "description": "The industry sector the project belongs to."
        },
        "owner": {
          "type": "object",
          "properties": {
            "uid": { "type": "string" },
            "name": { "type": "string" },
            "avatar": { "type": "string", "format": "uri" },
            "initials": { "type": "string" }
          },
          "required": ["uid", "name", "initials"]
        },
        "members": {
          "type": "array",
          "items": { "type": "string" },
          "description": "An array of user UIDs who are members of this project."
        },
        "totalMembers": {
          "type": "integer",
          "description": "The total number of members in the project."
        },
        "createdDate": {
          "type": "string",
          "format": "date",
          "description": "The date the project was created."
        },
        "lastEditDate": {
          "type": "string",
          "format": "date",
          "description": "The date the project was last edited."
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "A list of tags associated with the project."
        }
      },
      "required": ["id", "title", "description", "owner", "createdDate", "lastEditDate"]
    },
     "Task": {
      "title": "Task",
      "description": "Represents a single task or node within a project plan.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["Backlog", "In Progress", "Review", "Done"] },
        "priority": { "type": "string", "enum": ["Low", "Medium", "High", "Critical"] },
        "type": { "type": "string", "enum": ["Milestone", "Goal", "Epic", "Story", "Issue", "Sub-issue"] },
        "assignee": { "type": "object" },
        "startDate": { "type": "string" },
        "dueDate": { "type": "string" },
        "color": { "type": "string" },
        "position": { "type": "object", "properties": { "x": { "type": "number" }, "y": { "type": "number" } } },
        "next": { "type": "array", "items": { "type": "string" } },
        "prev": { "type": "array", "items": { "type": "string" } },
        "childrenIds": { "type": "array", "items": { "type": "string" } },
        "parentId": { "type": "string" },
        "size": { "type": "string" },
        "history": { "type": "array" },
        "attachments": { "type": "array" },
        "checklist": { "type": "array" },
        "cycleId": { "type": "string" }
      },
      "required": ["id", "title", "status", "priority", "type", "assignee", "startDate", "dueDate", "color", "position"]
    },
    "ProjectMember": {
      "title": "Project Member",
      "description": "Represents a user's membership in a project.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["owner", "member"] },
        "joinedAt": { "type": "object" }
      },
      "required": ["uid", "displayName", "role", "joinedAt"]
    },
    "Resource": {
      "title": "Resource",
      "description": "Represents a file or folder within a project's resources section.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["folder", "file"] },
        "fileType": { "type": "string", "description": "MIME type for files." },
        "size": { "type": "string" },
        "createdAt": { "type": "object", "description": "Firestore server timestamp." },
        "parentId": { "type": ["string", "null"] },
        "url": { "type": "string", "format": "uri" },
        "content": { "type": "string", "description": "JSON string for BlockNote documents." }
      },
      "required": ["id", "name", "type", "createdAt", "parentId"]
    },
    "Comment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Comment",
      "type": "object",
      "description": "Represents a comment on a post.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Comment entity."
        },
        "postId": {
          "type": "string",
          "description": "Reference to the Post this comment belongs to. (Relationship: Post 1:N Comment)"
        },
        "text": {
          "type": "string",
          "description": "The text content of the comment."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the comment was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "postId",
        "text",
        "createdAt"
      ]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Publicly accessible user profile information.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL for the user's avatar."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        }
      },
      "required": [
        "uid",
        "displayName"
      ]
    },
    "User": {
      "title": "User",
      "description": "Represents a user's private data and relationships.",
      "type": "object",
      "properties": {
        "profile": {
          "$ref": "#/backend/entities/UserProfile"
        },
        "friends": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "An array of UIDs of the user's friends."
        }
      },
      "required": [
        "profile"
      ]
    },
    "FriendRequest": {
      "title": "Friend Request",
      "description": "Represents a friend request between two users.",
      "type": "object",
      "properties": {
        "senderId": {
          "type": "string",
          "description": "UID of the user sending the request."
        },
        "receiverId": {
          "type": "string",
          "description": "UID of the user receiving the request."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "accepted",
            "declined"
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "senderId",
        "receiverId",
        "status",
        "createdAt"
      ]
    },
    "DirectMessage": {
      "title": "Direct Message Conversation",
      "description": "Represents a direct message conversation between users, which can be a 1-on-1 or a group chat.",
      "type": "object",
      "properties": {
        "participants": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "An array of UIDs of the participants in the DM."
        },
        "isGroup": {
          "type": "boolean",
          "description": "If true, this is a group chat."
        },
        "groupName": {
          "type": "string",
          "description": "The name of the group, if applicable."
        },
        "groupAvatar": {
            "type": "string",
            "format": "uri",
            "description": "URL for the group's avatar, if applicable."
        },
        "lastMessage": {
          "type": "object",
          "properties": {
            "text": { "type": "string" },
            "senderId": { "type": "string" },
            "timestamp": { "type": "object" }
          }
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp for when the conversation was created."
        }
      },
      "required": ["participants", "createdAt"]
    },
    "Message": {
      "title": "Message",
      "description": "Represents a single message within a conversation. It can be a standard text message or a special type, like a shared post or file.",
      "type": "object",
      "properties": {
        "senderId": {
          "type": "string",
          "description": "The UID of the user who sent the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the message was sent."
        },
        "type": {
          "type": "string",
          "enum": ["text", "postShare", "file"],
          "description": "The type of the message. Defaults to 'text'."
        },
        "text": {
          "type": "string",
          "description": "The text content of the message. Required for 'text' type messages."
        },
        "postId": {
          "type": "string",
          "description": "The ID of the shared post. Required for 'postShare' type messages."
        },
        "fileInfo": {
          "type": "object",
          "description": "Metadata for a shared file. Required for 'file' type messages.",
          "properties": {
            "name": { "type": "string" },
            "size": { "type": "number" },
            "type": { "type": "string" },
            "url": { "type": "string", "format": "uri" }
          },
           "required": ["name", "size", "type", "url"]
        }
      },
      "required": ["senderId", "createdAt", "type"]
    },
    "Article": {
      "title": "Article",
      "description": "Represents a news article written by a user.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the article."
        },
        "content": {
          "type": "string",
          "description": "The full content of the article, typically in a JSON format from a rich text editor."
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who wrote the article."
        },
        "authorName": {
          "type": "string",
          "description": "The display name of the author at the time of publishing."
        },
        "authorImage": {
          "type": "string",
          "format": "uri",
          "description": "URL of the author's avatar at the time of publishing."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the article was created."
        },
        "category": {
            "type": "string",
            "description": "The category of the article (e.g., Technology, Design)."
        },
        "readTime": {
            "type": "string",
            "description": "An estimated read time for the article (e.g., '6 min read')."
        }
      },
      "required": ["title", "content", "authorId", "authorName", "authorImage", "createdAt"]
    },
    "Server": {
      "title": "Server",
      "description": "Represents a server in the messaging app.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "ownerId": { "type": "string" },
        "iconUrl": { "type": "string", "format": "uri" },
        "members": { "type": "array", "items": { "type": "string" }}
      },
      "required": ["name", "ownerId", "members"]
    },
    "Channel": {
      "title": "Channel",
      "description": "Represents a channel within a server.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["text"] },
        "serverId": { "type": "string" }
      },
      "required": ["name", "type", "serverId"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information and their list of friends."
        }
      },
      {
        "path": "/friendRequests/{requestId}",
        "definition": {
          "entityName": "FriendRequest",
          "schema": {
            "$ref": "#/backend/entities/FriendRequest"
          },
          "description": "Stores friend requests between users."
        }
      },
      {
        "path": "/dms/{dmId}",
        "definition": {
          "entityName": "DirectMessage",
          "schema": { "$ref": "#/backend/entities/DirectMessage" },
          "description": "Stores metadata for a direct message conversation."
        }
      },
      {
        "path": "/dms/{dmId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": { "$ref": "#/backend/entities/Message" },
          "description": "Stores messages within a specific direct message conversation."
        }
      },
      {
        "path": "/servers/{serverId}",
        "definition": {
          "entityName": "Server",
          "schema": { "$ref": "#/backend/entities/Server" },
          "description": "Stores server information."
        }
      },
      {
        "path": "/servers/{serverId}/channels/{channelId}",
        "definition": {
          "entityName": "Channel",
          "schema": { "$ref": "#/backend/entities/Channel" },
          "description": "Stores channels within a specific server."
        }
      },
      {
        "path": "/servers/{serverId}/channels/{channelId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": { "$ref": "#/backend/entities/Message" },
          "description": "Stores messages within a specific server channel."
        }
      },
      {
        "path": "/posts/{postId}",
        "definition": {
          "entityName": "Post",
          "schema": {
            "$ref": "#/backend/entities/Post"
          },
          "description": "Stores individual posts. Includes 'postId' as the document ID.",
          "params": [
            {
              "name": "postId",
              "description": "The unique identifier for the post."
            }
          ]
        }
      },
      {
        "path": "/projects/{projectId}",
        "definition": {
          "entityName": "Project",
          "schema": {
            "$ref": "#/backend/entities/Project"
          },
          "description": "Stores individual projects for the Discover page."
        }
      },
      {
        "path": "/projects/{projectId}/tasks/{taskId}",
        "definition": {
          "entityName": "Task",
          "schema": { "$ref": "#/backend/entities/Task" },
          "description": "Stores tasks for a specific project."
        }
      },
      {
        "path": "/projects/{projectId}/members/{userId}",
        "definition": {
            "entityName": "ProjectMember",
            "schema": { "$ref": "#/backend/entities/ProjectMember" },
            "description": "Stores information about each member of a project."
        }
      },
      {
        "path": "/projects/{projectId}/resources/{resourceId}",
        "definition": {
          "entityName": "Resource",
          "schema": {
            "$ref": "#/backend/entities/Resource"
          },
          "description": "Stores files and folders for a specific project."
        }
      },
      {
        "path": "/posts/{postId}/comments/{commentId}",
        "definition": {
          "entityName": "Comment",
          "schema": {
            "$ref": "#/backend/entities/Comment"
          },
          "description": "Stores comments associated with a specific post. Includes 'commentId' as the document ID.",
          "params": [
            {
              "name": "postId",
              "description": "The unique identifier for the post that the comment belongs to."
            },
            {
              "name": "commentId",
              "description": "The unique identifier for the comment."
            }
          ]
        }
      },
      {
        "path": "/news/articles/{articleId}",
        "definition": {
          "entityName": "Article",
          "schema": { "$ref": "#/backend/entities/Article" },
          "description": "Stores news articles in a subcollection under a single 'news' document."
        }
      }
    ],
    "reasoning": "The data structure is designed to be simple and directly reflect the entity relationships described. It prioritizes straightforward access patterns and avoids unnecessary complexity. The structure enforces path-based ownership for comments within posts to ensure data integrity and simplifies security rules. All access is based on unauthenticated access with Post visibility being implicitly 'Public'.\n\nAuthorization Independence is achieved inherently because there is no explicit need for authorization besides the potential of requiring logged in status for posting. All posts are public. The use of subcollections for comments, while straightforward, doesn't necessitate denormalization in this public-access scenario. If comments needed role-based access, the post's member list or owner would be denormalized into the comment documents.\n\nThe structure supports list operations (QAPs) by leveraging the natural hierarchy. Listing comments for a specific post is efficient via a collection group query on the `/posts/{postId}/comments` subcollection. Listing posts themselves is also direct from `/posts`."
  }
}

    